server:
  port: 8002

spring:
  application:
    name: tracking-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: org.gjt.mm.mysql.Driver
    url: jdbc:mysql://localhost:3306/CargoTrackingSystem?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: 151220119

mybatis:
  type-aliases-package: com.nju.springcloud.entities
  # To avoid class with the same name